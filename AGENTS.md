# AGENTS.md — BIND

This repository is empty. No source code, tooling, or configuration has been set up yet.

## What to do before working here

- Confirm what this project is meant to be before creating any files.
- If adding a new project, establish the standard toolchain first (build, test, lint, typecheck) before writing application code.
- If this is a migration or restructuring, check whether existing code lives in another location.

## No conventions established yet

There is no:
- package manifest, lockfile, or dependency manager
- build/test/lint/format configuration
- CI workflow
- `.gitignore` or version control setup
- existing instruction files (AGENTS.md, CLAUDE.md, cursor rules, copilot instructions, opencode.json)

All conventions are to be determined.
